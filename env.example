# 네이버 키워드 수집 시스템 환경변수 설정
# 이 파일을 .env.local로 복사하고 실제 값으로 설정하세요

# ===========================================
# Supabase 설정
# ===========================================
# Supabase 프로젝트 URL (Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase 익명 키 (Settings → API → anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase 서비스 역할 키 (Settings → API → service_role)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# 네이버 검색광고 API (RelKwdStat)
# ===========================================
# JSON 배열 형식으로 여러 키 설정 가능
# 각 키는 label, accessLicense, secret, customerId, qps, daily 속성을 가져야 함
NAVER_SEARCHAD_KEYS='[
  {
    "label": "searchad-key-1",
    "accessLicense": "your-access-license-1",
    "secret": "your-secret-key-1",
    "customerId": "your-customer-id-1",
    "qps": 0.5,
    "daily": 8000
  },
  {
    "label": "searchad-key-2", 
    "accessLicense": "your-access-license-2",
    "secret": "your-secret-key-2",
    "customerId": "your-customer-id-2",
    "qps": 0.5,
    "daily": 8000
  }
]'

# ===========================================
# 네이버 오픈API (문서수 조회)
# ===========================================
# JSON 배열 형식으로 여러 키 설정 가능
# 각 키는 label, clientId, clientSecret, qps, daily 속성을 가져야 함
NAVER_OPENAPI_KEYS='[
  {
    "label": "openapi-key-1",
    "clientId": "your-client-id-1",
    "clientSecret": "your-client-secret-1",
    "qps": 3.0,
    "daily": 20000
  },
  {
    "label": "openapi-key-2",
    "clientId": "your-client-id-2", 
    "clientSecret": "your-client-secret-2",
    "qps": 3.0,
    "daily": 20000
  }
]'

# ===========================================
# 서버 보안 설정
# ===========================================
# 관리자 API 접근용 토큰 (강력한 랜덤 문자열 사용)
SERVER_TOKEN=your-secure-server-token-here

# ===========================================
# 데이터베이스 설정 (선택사항)
# ===========================================
# 데이터베이스 연결 풀 크기
DATABASE_POOL_SIZE=10

# 데이터베이스 타임아웃 (밀리초)
DATABASE_TIMEOUT=30000

# ===========================================
# 개발/테스트 설정 (선택사항)
# ===========================================
# 개발 모드에서 사용할 테스트 키워드
TEST_SEED_KEYWORDS='["마케팅", "SEO", "광고"]'

# 로그 레벨 (debug, info, warn, error)
LOG_LEVEL=info

# ===========================================
# 성능 튜닝 (선택사항)
# ===========================================
# 동시 처리 수 (연관키워드 수집)
RELATED_CONCURRENCY=2

# 동시 처리 수 (문서수 수집)
DOCS_CONCURRENCY=6

# 배치 크기 (연관키워드 수집)
RELATED_BATCH_SIZE=300

# 배치 크기 (문서수 수집)
DOCS_BATCH_SIZE=800

# ===========================================
# 알림 설정 (선택사항)
# ===========================================
# 슬랙 웹훅 URL (오류 알림용)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# 이메일 알림 설정
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
ALERT_EMAIL=admin@yourcompany.com